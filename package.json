{
  "name": "teamshare",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "sync:plugin-bin": "node scripts/sync-plugin-bin.mjs"
  }
}
