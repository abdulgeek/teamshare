# Local state on purpose (see README.md) — never commit it or the lock info.
*.tfstate
*.tfstate.*
.terraform.tfstate.lock.info

# Downloaded provider plugins — large, regenerated by `terraform init`.
.terraform/

# Never commit a plan file; it can embed resource attribute values.
*.tfplan
