{
  "name": "teamshare-plugin",
  "version": "0.4.1",
  "private": true,
  "type": "module",
  "scripts": { "test": "vitest run", "build": "echo 'no build step'" },
  "devDependencies": { "vitest": "^4.1.11" }
}
